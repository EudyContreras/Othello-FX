Refer to the main package for user related classes

The Example Agent classes extend AgentMove 
write your logic inside an agent class of your own
which extends AgentMove.

The AgentManager will allow you to run a desired agent

The Example move is class which can be use to implement your
own move.

The Settings class provides some settings as well as
some default useful constants.